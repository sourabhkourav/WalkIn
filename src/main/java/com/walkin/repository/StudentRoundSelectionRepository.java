package com.walkin.repository;

import com.walkin.entity.StudentRoundSelection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StudentRoundSelectionRepository extends JpaRepository<StudentRoundSelection, Integer> {
}
